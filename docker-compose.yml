services:
  kanban:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    volumes:
      - kanban-data:/app/data

volumes:
  kanban-data:
